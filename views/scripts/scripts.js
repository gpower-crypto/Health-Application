// show alert if user entered incorrect login credentials
var showAlert = document.getElementById("show-alert").value;
if(showAlert == "true"){
  alert("Sorry, Please enter correct username and password to login");
};

showAlert = false;
